package com.puertodeseado.repositorio;

import com.puertodeseado.entidades.RopaTalles;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RopaTallesRepositorio extends JpaRepository<RopaTalles, Integer> {
}
